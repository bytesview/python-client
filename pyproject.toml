[project]
name = "newsdataapi"
description = "Official Python client (SDK) for the Newsdata.io News API — fetch real-time, historical, crypto, and stock-market news via REST, with validation, retries, pagination, and typed exceptions."
dynamic = ["version"]
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "NewsData.io", email = "contact@newsdata.io" }]
requires-python = ">=3.8"

dependencies = [
    "requests>=2.25,<3",
]

keywords = [
    "news",
    "newsdata",
    "newsdata-io",
    "news-api",
    "rest-api",
    "api-client",
    "python-sdk",
    "sdk",
    "crypto-news",
    "financial-news",
    "market-news",
    "news-aggregator",
]

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

[project.optional-dependencies]
# Real-time WebSocket consumer (newsdataapi.NewsDataApiWebSocket). websockets 16
# requires Python >=3.10, so the extra is gated on that marker; the REST client
# itself still supports 3.8+ (on 3.8/3.9 the extra installs nothing and the
# consumer raises a clear "install the websocket extra" error).
websocket = ["websockets>=16,<17; python_version >= '3.10'"]

[project.urls]
Homepage = "https://newsdata.io"
Documentation = "https://newsdata.io/documentation"
Repository = "https://github.com/bytesview/python-client"
Issues = "https://github.com/bytesview/python-client/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Single source of truth for the version: __init__.py.
[tool.hatch.version]
path = "src/newsdataapi/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/newsdataapi"]

# PEP 735 dependency groups (uv reads these natively).
[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
    "responses>=0.25",
    "ruff>=0.6",
    "mypy>=1.10",
    "types-requests",
    "websockets>=16,<17; python_version >= '3.10'",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: live API tests requiring PYTEST_TOKEN env var",
]
# Skip integration tests by default; opt in with `pytest -m integration`
# or `-m ""` to run everything.
addopts = [
    "-ra",
    "--strict-markers",
    "-m", "not integration",
]

[tool.ruff]
line-length = 100
target-version = "py38"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "UP",   # pyupgrade
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "RET",  # flake8-return
    "PTH",  # flake8-use-pathlib
]

[tool.mypy]
python_version = "3.9"
strict = true
files = ["src/newsdataapi"]

# websockets is an optional dependency; when it isn't installed (e.g. a Python
# 3.8/3.9 env, where the extra's marker excludes it) treat the import as
# untyped rather than a hard error. It ships its own types when present.
[[tool.mypy.overrides]]
module = "websockets.*"
ignore_missing_imports = true
